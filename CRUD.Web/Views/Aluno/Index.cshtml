@model IEnumerable<CRUD.Model.AlunoModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SobreNome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SobreNome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idMatric }) |
            @Html.ActionLink("Details", "Details", new { id=item.idMatric }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idMatric })
        </td>
    </tr>
}

</table>
